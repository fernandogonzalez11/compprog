#include <bits/stdc++.h>

// macros
typedef long long int ll;
#define vec vector 
#define loop(i, a, b) for (int i = a; i < b; i++)
#define F first
#define S second
// constants
#define INF (1 << 30) + (1 << 25)

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	
	ll x; cin >> x;
	// loop(y, 1, x) {
	// 	int c = x^y;
	// 	cout << x << " " << y << " " << c << "\n";
	// 	cout << (x + y > c && x + c > y && y + c > x) << " ";
	// 	cout << ((x & y) > 0 && (x & ~y) > 0 && (y & ~x) > 0) << "\n";
	// }


	return 0;
}
